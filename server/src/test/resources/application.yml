spring:
  mvc:
    path match:
      matching-strategy: ant_path_matcher
  data:
    mongodb:
      port: 27017
      host: 127.0.0.1
      database: test
      repositories:
        type: auto
cors:
  originPatterns: http://localhost:4200